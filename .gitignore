# Build output
dist/
.astro/
node_modules/

# Env
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Local Claude Code settings — per-machine, not shared
.claude/settings.local.json

# Third-party reference material — full-page screencaptures and screen
# recordings of other companies' websites (arcade.software, amplemarket.com),
# ~43 MB of binaries. Kept local: not ours to republish, and not needed to
# build the site.
refencer image for ui/

# Reverse-engineered design specs of seven third-party products, used as
# input when writing DESIGN.md. The conclusions live in DESIGN.md; the
# source analyses stay local.
refrence design md files/
